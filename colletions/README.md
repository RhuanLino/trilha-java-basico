## Mission

At this code the mission is to learn how to use arrays, lists, map and set in Java.

## Folder Structure

The workspace contains two folders by default, where:

- `src`: the folder that has the .java file
